import { SummerTimeline } from '@/components/timeline/SummerTimeline';

export const metadata = { title: 'Summer Timeline — Student Dashboard' };

export default function SummerTimelinePage() {
  return <SummerTimeline />;
}
